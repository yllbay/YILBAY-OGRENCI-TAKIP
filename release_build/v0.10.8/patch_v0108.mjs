import fs from 'fs';
import path from 'path';
const ROOT='/tmp/yilbay0108';
const serverPath=path.join(ROOT,'app','server.js'),appPath=path.join(ROOT,'app','public','app.js');
let s=fs.readFileSync(serverPath,'utf8'),a=fs.readFileSync(appPath,'utf8');
s=s.replaceAll('0.10.7','0.10.8');a=a.replaceAll('0.10.7','0.10.8');fs.writeFileSync(path.join(ROOT,'app','VERSION'),'0.10.8\n','utf8');
function between(src,start,end,repl){const i=src.indexOf(start),j=src.indexOf(end,i+start.length);if(i<0||j<0)throw new Error('anchor '+start);return src.slice(0,i)+repl+src.slice(j)}
const instruction=`function lunaHomeworkDirectInstruction(){return \`LUNA MALİYET-OPTİMİZE ÖDEV ANALİZ PROTOKOLÜ:\nYüklenen öğrenci PDF/görselinin tamamını doğrudan analiz et; ilk sayfada durma ve tüm fiziksel sayfaları sırayla tara.\nÖNCE PDF'deki bölüm/test başlıklarını belirle. Soru numaraları yeni bölüm/testte 1'den yeniden başlayabilir. Aynı basılı soru numarası farklı bölüm/testlerde FARKLI sorudur.\nSoru kimliği kesinlikle yalnız basılı sayı değildir. Her fiziksel soru için sectionKey + printedNumber kullan. sectionKey aynı bölüm/test boyunca sabit, kısa ve benzersiz olsun. Örnek: sayi-problemleri:1, kesir-problemleri:1 ve bos-dolu-problemleri:1 üç ayrı sorudur. Başlık yoksa fallback olarak page<sayfa>:q<basılıNo> kullan. Kesme/görünme sırasını kimlik yapma.\nDOĞRU sorular için yalnız {sectionKey,section,page,printedNumber} kaydını correctQuestions listesine yaz; çözüm açıklaması üretme.\nBOŞ sorular için yalnız aynı kimlik alanlarını blankQuestions listesine yaz; neden tahmini yapma.\nUNCERTAIN sorular için yalnız aynı kimlik alanlarını uncertainQuestions listesine yaz; tahmin yapma.\nYALNIZ WRONG sorular için items içinde kimlik alanlarıyla birlikte studentAnswer, correctAnswer, approach, stepsSummary, firstErrorStep, errorCategory, conceptualIssue, arithmeticIssue, attentionIssue, methodQuality, unnecessarySteps, missingSteps, betterApproach, learningObjective, solutionConfidence ve kısa note üret.\nEl yazısında ilk yanlış adımı özellikle bul. PDF içinde güvenilir basılı cevap anahtarı varsa yerini answerKeyEvidence ile belirt. Güvenilir anahtar yoksa doğru/yanlış uydurma; uncertain yap.\nreasoningProfile yalnız WRONG sorulardan türetilsin ve kısa olsun.\n\`}\n`;
s=between(s,'function lunaHomeworkDirectInstruction(){','function validateLunaQuestionAccounting',instruction);
const helpers=`function normalizeHomeworkQuestionRef(x={}){const printedNumber=Math.max(0,Math.round(Number(x.printedNumber??x.question)||0)),page=Math.max(0,Math.round(Number(x.page)||0)),section=String(x.section||'').trim().slice(0,160),rawKey=String(x.sectionKey||section||'').trim().toLocaleLowerCase('tr-TR').normalize('NFKD').replace(/[\\u0300-\\u036f]/g,'').replace(/[^a-z0-9ıüğşöç]+/g,'-').replace(/^-+|-+$/g,'').slice(0,120),sectionKey=rawKey||('page'+(page||0));if(!printedNumber)return null;const questionId=sectionKey+':'+printedNumber,label=(section||sectionKey)+' / Soru '+printedNumber;return {questionId,sectionKey,section,page,printedNumber,label}}\nfunction normalizeHomeworkQuestionRefs(a=[]){const out=[],seen=new Set();for(const x of Array.isArray(a)?a:[]){const q=normalizeHomeworkQuestionRef(x);if(q&&!seen.has(q.questionId)){seen.add(q.questionId);out.push(q)}}return out}\nfunction finalizeHomeworkQuestionSets(x={}){const correctQuestions=normalizeHomeworkQuestionRefs(x.correctQuestions),blankQuestions=normalizeHomeworkQuestionRefs(x.blankQuestions),uncertainQuestions=normalizeHomeworkQuestionRefs(x.uncertainQuestions),items=[];const wrongRefs=[];for(const z of Array.isArray(x.items)?x.items:[]){if(String(z?.status||'wrong')!=='wrong')continue;const q=normalizeHomeworkQuestionRef(z);if(q){wrongRefs.push(q);items.push({...z,...q,question:q.printedNumber,questionLabel:q.label,status:'wrong'})}}const wrongQuestions=normalizeHomeworkQuestionRefs(wrongRefs),all=[...correctQuestions,...wrongQuestions,...blankQuestions,...uncertainQuestions],ids=all.map(q=>q.questionId),unique=new Set(ids),duplicateCount=Math.max(0,ids.length-unique.size),correct=correctQuestions.length,wrong=wrongQuestions.length,blank=blankQuestions.length,uncertain=uncertainQuestions.length,total=unique.size,classified=correct+wrong+blank+uncertain,scoreDenom=correct+wrong+blank,score=scoreDenom?Math.round(1000*correct/scoreDenom)/10:0,ok=total>0&&duplicateCount===0&&classified===total;return {...x,correctQuestions,wrongQuestions,blankQuestions,uncertainQuestions,items,correct,wrong,blank,uncertain,totalQuestions:total,classifiedQuestions:classified,scorePercent:score,questionIdentityDuplicateCount:duplicateCount,questionAccountingVerified:ok,correctQuestionNumbers:correctQuestions.map(q=>q.label),wrongQuestionNumbers:wrongQuestions.map(q=>q.label),blankQuestionNumbers:blankQuestions.map(q=>q.label),uncertainQuestionNumbers:uncertainQuestions.map(q=>q.label),needsTeacherReview:!!x.needsTeacherReview||!ok,autoFinalize:!!x.autoFinalize&&ok}}\n`;
const hs=s.indexOf('async function handleHomework(req,res){'),he=s.indexOf('function reportText',hs);if(hs<0||he<0)throw new Error('handle bounds');
const schema='Sadece geçerli ve KISA JSON döndür. Şema: {"confidence":number,"needsTeacherReview":boolean,"correctQuestions":[{"sectionKey":"string","section":"string","page":number,"printedNumber":number}],"blankQuestions":[aynı kimlik],"uncertainQuestions":[aynı kimlik],"items":[{"sectionKey":"string","section":"string","page":number,"printedNumber":number,"status":"wrong","studentAnswer":"string|null","correctAnswer":"string|null","approach":"string|null","stepsSummary":["string"],"firstErrorStep":"string|null","errorCategory":"conceptual|arithmetic|attention|method|incomplete|uncertain","conceptualIssue":"string|null","arithmeticIssue":"string|null","attentionIssue":"string|null","methodQuality":"efficient|acceptable|inefficient|incorrect|uncertain","unnecessarySteps":["string"],"missingSteps":["string"],"betterApproach":"string|null","learningObjective":"string|null","solutionConfidence":number,"note":"string|null"}],"reasoningProfile":{"strengths":["string"],"recurringErrors":["string"],"conceptualGaps":["string"],"proceduralGaps":["string"],"attentionPatterns":["string"],"recommendedActions":["string"],"summary":"string"},"summary":"string","answerKeyFound":boolean,"answerKeySource":"manual|embedded_same_page|embedded_adjacent_page|embedded_end_pages|ambiguous|none","answerKeyOriginDocument":"manual|student_pdf|ambiguous|none","answerKeyConfidence":number,"answerKeyEvidence":"string","analyzedStudentPages":[number]}';
const handle=`async function handleHomework(req,res){\n  const body=await readJson(req,18*1024*1024);\n  const {assignment,answerKey=null,resourceContext=null,studentFileSource="local"}=body;\n  let studentFile;try{studentFile=await resolveStudentHomeworkFile(body)}catch(e){return json(res,400,{ok:false,error:e.message})}\n  if(!integrationStatus().openai.configured)return json(res,400,{ok:false,error:"Ödev analizi için OpenAI API anahtarı gerekli"});\n  const expectedStudentPages=estimatePdfPageCount(studentFile.base64,studentFile.mimeType);\n  const safeContext=resourceContext?{resourceId:resourceContext.resourceId||null,course:resourceContext.course||assignment?.course||"",unit:resourceContext.unit||"",topic:resourceContext.topic||assignment?.topic||"",level:resourceContext.level||"",title:resourceContext.title||assignment?.title||""}:null;\n  const instructions=lunaHomeworkDirectInstruction()+${JSON.stringify('\n'+schema)};\n  const meta={assignment:{id:assignment?.id||null,course:assignment?.course||"",topic:assignment?.topic||"",title:assignment?.title||""},resourceContext:safeContext,manualAnswerKey:answerKey||null};\n  const content=[{type:"input_text",text:JSON.stringify(meta)},{type:"input_text",text:"ÖĞRENCİ ÖDEV DOSYASI. Tüm sayfaları tara. Bölüm/test başlıklarını ayır; soru numarası her bölümde yeniden başlayabilir. Doğru ve boşları kısa tut, yalnız yanlışları ayrıntılandır."},{type:"input_file",filename:studentFile.fileName,file_data:"data:"+studentFile.mimeType+";base64,"+studentFile.base64}];\n  const ai=await openaiRequest({instructions,input:[{role:"user",content}],reasoning:"low"});\n  let parsed=normalizeHomeworkAnalysis(parseJsonText(ai.text));\n  parsed=finalizeHomeworkQuestionSets(parsed);\n  parsed=validateLunaDirectAnswerKey(parsed,answerKey);\n  parsed=validateHomeworkPageCoverage(parsed,expectedStudentPages,0);\n  parsed=validateLunaQuestionAccounting(parsed);\n  parsed=deriveHomeworkReviewReasons(parsed);\n  const cost=appendUsage("homework_analysis",ai.model,ai.data?.usage||{}, {studentId:assignment?.studentId||null,assignmentId:assignment?.id||null});\n  parsed.studentFileSource=studentFileSource;parsed.studentFileDriveUsed=!!studentFile.driveUsed;parsed.analysisArchitecture="direct_luna_wrong_only_composite_question_id";\n  writeHomeworkDiagnostic(parsed,cost,assignment);\n  return json(res,200,{ok:true,analysis:parsed,autoFinalize:parsed.autoFinalize,usage:ai.data?.usage||null,cost});\n}\n\n`;
s=s.slice(0,hs)+helpers+handle+s.slice(he);
const diagStart=s.indexOf('function writeHomeworkDiagnostic('),diagEnd=s.indexOf('function validateHomeworkQuestionAccounting',diagStart);if(diagStart<0||diagEnd<0)throw new Error('diag');
const diag=`function writeHomeworkDiagnostic(analysis={},cost=null,assignment=null){const safe={timestamp:new Date().toISOString(),assignmentId:assignment?.id??null,expectedStudentPages:Number(analysis.expectedStudentPages)||0,analyzedStudentPages:Array.isArray(analysis.analyzedStudentPages)?analysis.analyzedStudentPages.slice(0,500):[],missingStudentPages:Array.isArray(analysis.missingStudentPages)?analysis.missingStudentPages.slice(0,500):[],pdfCoverageVerified:!!analysis.pdfCoverageVerified,answerKeyFound:!!analysis.answerKeyFound,answerKeySource:String(analysis.answerKeySource||'none'),answerKeyConfidence:Number(analysis.answerKeyConfidence)||0,confidence:Number(analysis.confidence)||0,needsTeacherReview:!!analysis.needsTeacherReview,autoFinalize:!!analysis.autoFinalize,totalQuestions:Number(analysis.totalQuestions)||0,correct:Number(analysis.correct)||0,wrong:Number(analysis.wrong)||0,blank:Number(analysis.blank)||0,uncertain:Number(analysis.uncertain)||0,scorePercent:Number(analysis.scorePercent)||0,classifiedQuestions:Number(analysis.classifiedQuestions)||0,questionAccountingVerified:!!analysis.questionAccountingVerified,questionIdentityDuplicateCount:Number(analysis.questionIdentityDuplicateCount)||0,reviewReasons:Array.isArray(analysis.reviewReasons)?analysis.reviewReasons.slice(0,20):[],analysisArchitecture:String(analysis.analysisArchitecture||''),correctQuestionNumbers:Array.isArray(analysis.correctQuestionNumbers)?analysis.correctQuestionNumbers.slice(0,1000):[],wrongQuestionNumbers:Array.isArray(analysis.wrongQuestionNumbers)?analysis.wrongQuestionNumbers.slice(0,1000):[],blankQuestionNumbers:Array.isArray(analysis.blankQuestionNumbers)?analysis.blankQuestionNumbers.slice(0,1000):[],uncertainQuestionNumbers:Array.isArray(analysis.uncertainQuestionNumbers)?analysis.uncertainQuestionNumbers.slice(0,1000):[],costTry:Number(cost?.try)||0,secretsCollected:false,contentCollected:false};fs.mkdirSync(runtime,{recursive:true});fs.writeFileSync(path.join(runtime,'last_homework_analysis.json'),JSON.stringify(safe,null,2),'utf8');return safe}\n`;
s=s.slice(0,diagStart)+diag+s.slice(diagEnd);
s=s.replace("function reportNums(a){return Array.isArray(a)?[...new Set(a.map(Number).filter(n=>Number.isInteger(n)&&n>0))].sort((x,y)=>x-y).slice(0,1000):[]}","function reportNums(a){return Array.isArray(a)?[...new Set(a.map(v=>String(v||'').trim()).filter(Boolean))].slice(0,1000):[]}");
s=s.replace("question:Number(z.question)||0,studentAnswer:","question:reportText(z.questionLabel||z.question,180),studentAnswer:");
const delFn=`function handleHomeworkReportDelete(u,res){const m=String(u.pathname||'').match(/^\\/api\\/reports\\/homework-pdf\\/([0-9]+_[a-f0-9]{1,16})$/i);if(!m)return json(res,400,{ok:false,error:'Geçersiz PDF kimliği'});const f=path.join(runtime,'homework_reports',m[1]+'.pdf');fs.rmSync(f,{force:true});return json(res,200,{ok:true,deleted:true})}\n`;
const dl=s.indexOf('function handleHomeworkReportDownload');s=s.slice(0,dl)+delFn+s.slice(dl);
s=s.replace('if(u.pathname.startsWith("/api/reports/homework-pdf/")&&req.method==="GET") return handleHomeworkReportDownload(u,res);','if(u.pathname.startsWith("/api/reports/homework-pdf/")&&req.method==="DELETE") return handleHomeworkReportDelete(u,res);\n    if(u.pathname.startsWith("/api/reports/homework-pdf/")&&req.method==="GET") return handleHomeworkReportDownload(u,res);');
const rStart=a.indexOf('function results(){'),rEnd=a.indexOf('window.resultModal=',rStart);if(rStart<0||rEnd<0)throw new Error('results');
const results=`function results(){\n const rows=db.results.slice().reverse().map(r=>\`<tr><td><div class="cell-title">\${studentName(r.studentId)}</div></td><td>\${r.kind}</td><td><div class="cell-title">\${r.topic}</div><div class="cell-sub">\${r.course}</div></td><td><span class="badge \${scoreClass(r.score)}">%\${r.score}</span></td><td>\${r.date}</td></tr>\`).join("");\n const analyses=db.homeworkAnalyses.slice().reverse().slice(0,50).map(x=>\`<tr><td>\${studentName(x.studentId)}</td><td>\${x.course} / \${x.topic}</td><td><span class="badge \${x.needsTeacherReview&&!x.teacherReviewed?"mid":"good"}">\${x.needsTeacherReview&&!x.teacherReviewed?"Öğretmen Kontrolü":x.teacherReviewed?"Öğretmen Onaylı":"AI Kesinleştirdi"}</span></td><td>%\${Math.round(Number(x.teacherScore??x.scorePercent)||0)}</td><td>%\${Math.round((Number(x.confidence)||0)*100)}</td><td>\${Number(x.costTry||0).toFixed(4)} TL</td><td><button class="btn danger small" onclick="deleteHomeworkAnalysis(\${x.id})">Sil</button></td></tr>\`).join("");\n const clear= db.homeworkAnalyses.length?'<button class="btn danger small" onclick="clearHomeworkAnalyses()">Tüm Analiz Geçmişini Temizle</button>':'';\n shell(\`\${pageHead("Başarı Sonuçları","Ödev ve online sınav başarılarını kaydedin. Adaptif tekrar eşiği %"+db.threshold,\`<button class="btn primary" onclick="resultModal()">+ Sonuç Gir</button>\`)}\${rows?tableWrap(\`<table><thead><tr><th>Öğrenci</th><th>Tür</th><th>Konu</th><th>Başarı</th><th>Tarih</th></tr></thead><tbody>\${rows}</tbody></table>\`):emptyState("Sonuç bulunamadı","İlk ödev veya sınav sonucunu girin.")}<div class="section"><div class="section-head"><h2>AI Ödev Analiz Geçmişi</h2><div>\${clear}</div></div>\${analyses?tableWrap(\`<table><thead><tr><th>Öğrenci</th><th>Ders / Konu</th><th>Durum</th><th>Başarı</th><th>AI Güveni</th><th>Maliyet</th><th>İşlem</th></tr></thead><tbody>\${analyses}</tbody></table>\`):emptyState("AI analiz geçmişi yok","İlk AI ödev taramasından sonra burada görünecek.")}</div>\`,"results")\n}\nwindow.deleteHomeworkAnalysis=async id=>{const x=db.homeworkAnalyses.find(v=>Number(v.id)===Number(id));if(!x||!confirm('Bu AI analiz kaydı silinsin mi? Akademik sonuç kaydı korunur.'))return;if(x.pdfReportUrl){try{await fetch(x.pdfReportUrl,{method:'DELETE'})}catch{}}db.homeworkAnalyses=db.homeworkAnalyses.filter(v=>Number(v.id)!==Number(id));save();results()}\nwindow.clearHomeworkAnalyses=async()=>{if(!db.homeworkAnalyses.length||!confirm('Tüm AI ödev analiz geçmişi ve yerel PDF karneleri silinsin mi? Akademik sonuçlar korunur.'))return;for(const x of db.homeworkAnalyses){if(x.pdfReportUrl){try{await fetch(x.pdfReportUrl,{method:'DELETE'})}catch{}}}db.homeworkAnalyses=[];save();results()}\n`;
a=a.slice(0,rStart)+results+a.slice(rEnd);
fs.writeFileSync(serverPath,s,'utf8');fs.writeFileSync(appPath,a,'utf8');console.log('v0.10.8 patch applied');
