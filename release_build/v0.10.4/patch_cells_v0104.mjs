import fs from 'fs';
import path from 'path';
const ROOT='/tmp/yilbay0104';
const mp=path.join(ROOT,'cells','micro-cell-manifest.json');
const m=JSON.parse(fs.readFileSync(mp,'utf8'));
if(m.version!=='0.10.3'||m.protocol!=='micro-cell-v2-ast')throw new Error('Expected verified v0.10.3 source');
for(const e of [...m.frontend,...m.backend]){const p=path.join(ROOT,e.file);let s=fs.readFileSync(p,'utf8');s=s.replaceAll('0.10.3','0.10.4');fs.writeFileSync(p,s,'utf8')}
const by=(arr,n)=>arr.find(x=>x.name===n);
const handle=by(m.backend,'handleHomework'),writer=by(m.backend,'writeHomeworkDiagnostic'),submit=by(m.frontend,'submitHomeworkAnalysis');
if(!handle||!writer||!submit)throw new Error('required cells missing');
const scopeInstruction=`function homeworkAssignmentScopeInstruction(){return \`ATAMA SORU KAPSAMI PROTOKOLÜ:\nÖnce bu atamaya ait soru kümesini belirle; sonra yalnız bu kümeyi puanla ve çözüm yaklaşımını raporla. Kimlik olarak yalnız PDF üzerinde basılı/orijinal soru numaralarını kullan. Sayfada görünme sırası, kırpma sırası veya modelin kendi sayacı soru kimliği değildir. assignment.title, course, topic, resourceContext.title, unit, questionCount ve varsa test adı/başlığı ile eşleştir. PDF birden fazla test içeriyorsa yalnız atamaya ait testi seç. matchedQuestionNumbers benzersiz ve artan basılı soru numaraları olmalı. Beklenen questionCount varsa tam o kadar soru güvenle eşleşmedikçe assignmentMatchFound=false veya assignmentMatchConfidence düşük ver ve needsTeacherReview=true yap. Eşleşmeyen diğer testleri doğru/yanlış/boş toplamlarına katma. assignmentMatchEvidence hangi başlık/test/sayfa işaretleriyle eşleştiğini kısa açıklar.\`}\n`;
const scopeValidator=`function validateHomeworkAssignmentScope(analysis={},expected=0){const nums=Array.isArray(analysis.matchedQuestionNumbers)?analysis.matchedQuestionNumbers.map(Number).filter(n=>Number.isInteger(n)&&n>0):[];const unique=[...new Set(nums)].sort((a,b)=>a-b);const n=Math.max(0,Math.round(Number(expected)||0));const duplicateCount=Math.max(0,nums.length-unique.length);const confidence=Math.max(0,Math.min(1,Number(analysis.assignmentMatchConfidence)||0));const declared=analysis.assignmentMatchFound===true;const countOk=!n||unique.length===n;const found=declared&&duplicateCount===0&&countOk&&confidence>=0.75;return {...analysis,matchedQuestionNumbers:unique,matchedQuestionCount:unique.length,assignmentMatchConfidence:Math.round(confidence*1000)/1000,assignmentMatchFound:found,assignmentScopeDuplicateCount:duplicateCount,assignmentScopeCountVerified:countOk,assignmentMatchEvidence:String(analysis.assignmentMatchEvidence||'').slice(0,500),needsTeacherReview:!!analysis.needsTeacherReview||!found,autoFinalize:!!analysis.autoFinalize&&found}}\n`;
let hs=fs.readFileSync(path.join(ROOT,handle.file),'utf8');
hs=hs.replace('async function handleHomework(req,res){',scopeInstruction+scopeValidator+'async function handleHomework(req,res){');
hs=hs.replace('  const reasoningInstruction=homeworkReasoningInstruction();','  const reasoningInstruction=homeworkReasoningInstruction();\n  const assignmentScopeInstruction=homeworkAssignmentScopeInstruction();');
hs=hs.replace('${reasoningInstruction}\nCevap anahtarı kanıtı','${reasoningInstruction}\n${assignmentScopeInstruction}\nCevap anahtarı kanıtı');
hs=hs.replace('"totalQuestions":number,','"assignmentMatchFound":boolean,"assignmentMatchConfidence":number,"assignmentMatchEvidence":"string","matchedTestName":"string|null","matchedQuestionNumbers":[number],"totalQuestions":number,');
hs=hs.replace('const parsed=deriveHomeworkReviewReasons(validateHomeworkQuestionAccounting(validateHomeworkPageCoverage(validateHomeworkAnswerKeyEvidence(validateHomeworkQuestionCount(normalizeHomeworkAnalysis(parseJsonText(ai.text)),resourceContext?.questionCount),answerKey,resourceContext),expectedStudentPages,expectedSourcePages)));','const parsed=deriveHomeworkReviewReasons(validateHomeworkQuestionAccounting(validateHomeworkAssignmentScope(validateHomeworkPageCoverage(validateHomeworkAnswerKeyEvidence(validateHomeworkQuestionCount(normalizeHomeworkAnalysis(parseJsonText(ai.text)),resourceContext?.questionCount),answerKey,resourceContext),expectedStudentPages,expectedSourcePages),resourceContext?.questionCount)));');
if(!hs.includes('validateHomeworkAssignmentScope')||!hs.includes('ATAMA SORU KAPSAMI PROTOKOLÜ'))throw new Error('scope patch failed');
fs.writeFileSync(path.join(ROOT,handle.file),hs,'utf8');
let ws=fs.readFileSync(path.join(ROOT,writer.file),'utf8');
ws=ws.replace('sourcePageCountStatus:Number(analysis.expectedSourcePages)>0?\'known\':\'unknown\',','sourcePageCountStatus:Number(analysis.expectedSourcePages)>0?\'known\':\'unknown\',assignmentMatchFound:!!analysis.assignmentMatchFound,assignmentMatchConfidence:Number(analysis.assignmentMatchConfidence)||0,matchedQuestionCount:Number(analysis.matchedQuestionCount)||0,matchedQuestionNumbers:Array.isArray(analysis.matchedQuestionNumbers)?analysis.matchedQuestionNumbers.slice(0,500):[],assignmentScopeCountVerified:!!analysis.assignmentScopeCountVerified,assignmentScopeDuplicateCount:Number(analysis.assignmentScopeDuplicateCount)||0,');
fs.writeFileSync(path.join(ROOT,writer.file),ws,'utf8');
let ss=fs.readFileSync(path.join(ROOT,submit.file),'utf8');
ss=ss.replace('<div class="section"><h3>Sayfa Kapsamı</h3>','<div class="section"><h3>Atama Soru Kapsamı</h3><div class="cell-sub">Eşleşen soru sayısı: ${an.matchedQuestionCount||0}${an.matchedQuestionNumbers?.length?` · Basılı numaralar: ${an.matchedQuestionNumbers.join(", ")}`:""}<br>${an.assignmentMatchEvidence||"—"}</div></div>\n   <div class="section"><h3>Sayfa Kapsamı</h3>');
fs.writeFileSync(path.join(ROOT,submit.file),ss,'utf8');
const frontend=m.frontend.map(e=>fs.readFileSync(path.join(ROOT,e.file),'utf8')).join('');const backend=m.backend.map(e=>fs.readFileSync(path.join(ROOT,e.file),'utf8')).join('');
fs.writeFileSync(path.join(ROOT,'app','public','app.js'),frontend,'utf8');fs.writeFileSync(path.join(ROOT,'app','server.js'),backend,'utf8');fs.writeFileSync(path.join(ROOT,'app','VERSION'),'0.10.4\n','utf8');
console.log('v0.10.4 assignment scope patch applied');